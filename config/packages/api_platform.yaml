api_platform:
    mapping:
        paths: ['%kernel.project_dir%/config/api_platform/resources.yml']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]

    show_webby: false

    metadata_backward_compatibility_layer: false

    defaults:
        route_prefix: ~

